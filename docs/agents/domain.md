# Domain documentation

This project uses a single-context layout: a root `CONTEXT.md` glossary and architectural decisions under `docs/adr/`, when present.

Read relevant domain documentation before exploring or changing the project. If these files do not exist, proceed without requesting that they be created. Reuse glossary vocabulary and flag conflicts with existing architectural decisions.
